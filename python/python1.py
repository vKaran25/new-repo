#dictionary = a changable onordered collection of unique key:value  pairs fast coz hashing ,allows us to access value quickly

#capitals = {'USA':'washington dc','INDIA':'new delhi','china':'bejing'}

#print(capitals['USA']) #approach not safe coz it will not tell if the kwy is there or not 

#indexing = index operator[] = gives access to a sequence element(str,list,tuple)

#name = 'karan Verma'
#if(name[0].islower()):
#    name = name.capitalize()

#print(name)

#name = 'karan verma'

#firstName = name[:5].upper()
#print(firstName)

#lastName = name[5:].upper()
#print(lastName)

#lastCharacter = name[-1]
#print(lastCharacter)

#....................

#function = it is a block of code which is executed only when it is called 

#def hello(name):                 #function syntax , something defined in a function is called its parameter
#    print("hello " + name )

#function call
#hello("karan")      #info which is sent to a function is called a argument 

#name = "b praak"
#hello(name)

#two arguments cant be sent to a function untill and unless it has two functions 

#def info(firstName,lastName,age):
#    print("first name is " + firstName)
#    print("last name is " + lastName)
#    print("age is "+ str(age) )
#    print("Have a nice day")

#info("karan","verma",19)

#def add(a,b):
#    print(a+b)

#add(2,3)

#.............................

#return statement = functions send python values/objects back to caller.
#these values are called return value

#def multiply(a,b):
#    result = a*b
#    return result

#print(multiply(6,8))

#nested function calls = function calls inside other function calls innermost function calls are resolved
#                        first returned value used as a argument for the next outher function

#num = input("enter your number")
#num = float(num)
#num = abs(num)
#num = round(num)
#print(num)

#below function is same to above function but it is nested 

#print(round(abs(float(input("enter your number")))))

#.............................

#variable scope = region a variable is recoganizable 
#                 a local and global scope variable can be constructed 
# for ex variables created in a function is locallay scoped variable can be constructed 























